const ForPersonalIcon = ({ className }: { className: string }) => {
  return (
    <img
      src="/forPersonal.png"
      alt="login"
      className={className}
    />
  );
};

export default ForPersonalIcon;
