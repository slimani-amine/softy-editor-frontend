const ForSchoolIcon =({ className }: { className: string }) => {
  return (
    <img
      src="/forSchool.png"
      alt="login"
      className={className}
    />
  );
};

export default ForSchoolIcon;
