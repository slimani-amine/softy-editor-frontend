const ForPersonnelIcon = ({ className }: { className: string }) => {
  return (
    <img
      src="/forPersonnel.png"
      alt="login"
      className={className}
    />
  );
};

export default ForPersonnelIcon;
