const ForTeamIcon = ({ className }: { className: string }) => {
  return (
    <img
      src="/forTeam.png"
      alt="login"
      className={className + "h-full w-full"}
      
    />
  );
};

export default ForTeamIcon;
